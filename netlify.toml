[build]
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist"

[context.production.environment]
  CI = "false"
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
